#include <stdio.h>


int main(void) {
    int num;
    printf("Insira um número: ");
    scanf("%d", &num);
    printf("O sucessor e o antecessor do número %d é : %d e %d\n", num, (num+1), (num-1));

    return 0;
}
