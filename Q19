#include <stdio.h>

int main(void) {
    int numero;
    printf("Insira um número: ");
    scanf("%d", &numero);
    (numero % 2) ? printf("O número é ímpar") : printf("O número é par");
    return 0;
}
