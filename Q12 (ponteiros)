#include <stdio.h>

void troca(int *x, int *y) {
    int temp = *x;
    *x = *y;
    *y = temp;
}


int main(void) {
    int a, b;
    printf("Insira os valores de a e b: ");
    scanf("%d %d", &a, &b);
    troca(&a, &b);
    printf("Os valores trocados são: %d e %d\n", a, b);
    return 0;
}
